@model TRMDesktopUserI.Library.Models.Put.Goal
@{
    ViewBag.Title = "Production";
}
<script src="~/Scripts/jquery-3.3.1.min.js"></script>
<script src="~/Scripts/bootstrap.min.js"></script>
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<h2>Production</h2>
<div class="container" style="margin-top:3%">
    <a href="#" class="btn btn-info" onclick="AddNewProductionGoal(0)">Add New Goal</a> <br />
    <table class="table table-striped">
        <thread>
            <tr>
                <th>Product ID</th>
                <th>Target</th>
                <th>Product</th>
                <th>Production</th>
                <th>Action(Edit)</th>
                <th>Action(Delete)</th>
                


            </tr>
        </thread>
        <tbody id="SetGoalList">
            <tr id="LoadingStatus" style="color:red"></tr>
        </tbody>
    </table>

</div>

<div class="modal-fade" id="MyModal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <a href="#" class="close" data-dismiss="modal">&times;</a>
                <h4 id="ModalTitle"></h4>
            </div>
            <div class="modal-body">
                <form id="form">
                    <fieldset id="SubmitForm">
                        @Html.HiddenFor(m => m.productid, new { @id = "productid" })
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Product, new { @id = "Product", @class = "form-control", @placeholder = "Name*" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.target, new { @id = "target", @class = "form-control", @placeholder = "target" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Product, new { @id = "Product", @class = "form-control", @placeholder = "Product" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(m => m.Production, new { @id = "Production", @class = "form-control", @placeholder = "Production" })
                        </div>
                        @*   <div class="form-group">
            @Html.DropDownList(m => m.producid,ViewBag.ListOfProducts as SelectList,"--Select Product--", new { })
        </div>*@
                        <div class="form-group">
                            <a href='#' class="btn btn-block btn-danger" id="SaveGoalRecord">Save</a>
                        </div>
                    </fieldset>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    $("#LoadingStatus").html("Loading....");
    $.get("/Production/GetGoalList", null, DataBind);
    function DataBind(GoalList) {
        var SetGoal = @("#SetGoalList");
        for (var i = 0; i < GoalList.length; i++) {
            var Data = "<tr class='row_" + GoalList[i].productid + "'>" +
                "<td>" + GoalList[i].productid + "</td>" +
                "<td>" + GoalList[i].target + "</td>" +
                "<td>" + GoalList[i].Product + "</td>" +
                "<td>" + GoalList[i].Production + "</td>" +
                "<td>" + "<a href='#' class='btn btn-warning' onclick='EditGoalRecord(0)'><span class='glyphicon glyphicon-edit'></span></a>" + "</td>" +
                "<td>" + "<a href='#' class='btn btn-danger' onclick='DeleteGoalRecord(0)'><span class='glyphicon glyphicon-trash'></span></a>" + "</td>" +
                "</tr>";
            SetData.append(Data);
            $("#LoadingStatus").html(" ");
        }
    }
    //Show The Popup Modal For Add New Goal
    function AddNewProductionGoal(productid) {
        $("#form")[0].reset();
        $("#ModalTitle").html("Add New Goal");
        $("#MyModal").modal();
    }
</script>